#new_lst = list(map(countries.upper, ['a', 'b', 'c']))
   #print(new_lst)
#country_upper()

#new_lst = map(country_upper, countries)
   #return [x.upper() for x in new_lst]
#country_upper('lst')

#names_upper = map(lambda name: name.upper(), names)
#print(list(names_upper))